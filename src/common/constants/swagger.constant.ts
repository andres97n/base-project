export const SWAGGER_TITLE = 'New Base Project';

export const SWAGGER_DESCRIPTION =
  'Full Config Project — REST API with JWT auth, caching, rate limiting and a standard response envelope.';

export const SWAGGER_VERSION = '1.0';

export const SWAGGER_CONTACT_NAME = 'API Support';

export const SWAGGER_CONTACT_URL = '';

export const SWAGGER_CONTACT_EMAIL = '';

// Security scheme name referenced by @ApiBearerAuth() on protected endpoints.
export const SWAGGER_BEARER_AUTH_NAME = 'access-token';

// Path where the Swagger UI is mounted (relative to the global prefix).
export const SWAGGER_DOC_PATH = 'docs';
